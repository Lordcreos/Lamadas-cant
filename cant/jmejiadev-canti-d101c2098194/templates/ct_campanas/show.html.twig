{% extends 'base.html.twig' %}

{% block title %}CtCampanas{% endblock %}

{% block body %}
    <h1>CtCampanas</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ct_campana.id }}</td>
            </tr>
            <tr>
                <th>CamNombre</th>
                <td>{{ ct_campana.camNombre }}</td>
            </tr>
            <tr>
                <th>CamEstado</th>
                <td>{{ ct_campana.camEstado ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('ct_campanas_index') }}">back to list</a>

    <a href="{{ path('ct_campanas_edit', {'id': ct_campana.id}) }}">edit</a>

    {{ include('ct_campanas/_delete_form.html.twig') }}
{% endblock %}
